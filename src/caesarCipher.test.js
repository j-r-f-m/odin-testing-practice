/* import { ceasarCipher } from "./caesarCipher";
test("shift string elements by key", () => {
  expect(ceasarCipher("hallo du", 2)).toBe("jcnnq fw");
});

test("shift string elements by key", () => {
  expect(ceasarCipher("defend the east wall of the castle", 1)).toBe(
    "efgfoe uif fbtu xbmm pg uif dbtumf"
  );
});

test("shift string elements by key", () => {
  expect(ceasarCipher("abcdefghijklmnopqrstuvwxyz", -1)).toBe(
    "bcdefghijklmnopqrstuvwxyza"
  );
});

test("shift string elements by key", () => {
  expect(ceasarCipher("abcdefghijklmnopqrstuvwxyz", -2)).toBe(
    "cdefghijklmnopqrstuvwxyzab"
  );
});

test("shift string elements by key", () => {
  expect(ceasarCipher("defend the east wall of the castle.", 1)).toBe(
    "efgfoe uif fbtu xbmm pg uif dbtumf."
  );
});

test("shift string elements by key", () => {
  expect(ceasarCipher("defend the, east wall of the castle.", 1)).toBe(
    "efgfoe uif, fbtu xbmm pg uif dbtumf."
  );
});

test("shift string elements by key", () => {
  expect(ceasarCipher("Defend the, East wall of the castle.", 1)).toBe(
    "Efgfoe uif, Fbtu xbmm pg uif dbtumf."
  );
});
 */
